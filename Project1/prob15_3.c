/*
#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

int main() {
	int arr[10];
	int temp;
	int j = 0;
	int k = 9;

	for(int i = 0; i < 10; i++) {
		printf("Enter an integer : ");
		int x = scanf("%d", &temp);
		if(temp % 2 != 0) {
			arr[j] = temp;
			j++;
		} else {
			arr[k] = temp;
			k--;
		}
	}

	printf("The array is : ");

	for(int i = 0; i < 10; i++) {
		printf("%d ", arr[i]);
	}

	return 0;
}

*/

