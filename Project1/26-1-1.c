/*
#include <stdio.h>

#define PLUS(a,b,c) ((a)+(b)+(c))
#define MULTIPLY(a,b,c) ((a)*(b)*(c))

int main(void) {
	int x = 2, y = 3, z = 4;
	int sum = PLUS(x, y, z);
	int product = MULTIPLY(x, y, z);

	printf("Sum: %d\n", sum);
	printf("Product: %d\n", product);

	return 0;
}
*/

